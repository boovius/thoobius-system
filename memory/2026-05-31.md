

## 2026-05-31 late-session BELT coordination notes

- Josh asked for a Gamma.ai slide-deck skeleton for presenting BELT to peers. Provided a 16-slide skeleton framing BELT as a project development viability planning tool: problem, definition, origin, audience, workflow, current features, AI gap-fill, emissions/carbon-accounting direction, current build state, why it matters, asks for testers/relationship-builders/designers/carbon accountants, feedback questions, roadmap, and closing line: “turning ‘this could work’ into ‘here’s what would have to be true.’”
- BELT PR coordination today:
  - PR #116 was merged. Tank’s AI gap-fill adapter/schema work landed; previous follow-up extracted schema and added docs.
  - PR #117 remains the AI gap-fill orchestration seam. Kaylee reviewed it and said no architecture blocker after rebase, but recommended keeping it draft until it consumes the real OpenAI provider from #120 or clearly gates/labels heuristic preview behavior. Must-fix before leaving draft: integrate #120 provider or gate heuristic, plus seam-level API tests for `/ai-gap-fill/plan` and `/ai-gap-fill/apply`.
  - PR #120 is the server-side OpenAI provider wiring. Recommended to merge/review #120 first, then rebase/integrate #117 on top. Luther was delegated to review #120, then later delegated to integrate #120’s OpenAI adapter into #117 after #120 merged.
  - PR #119 is UI cleanup around geography/transport/outputs. Kaylee re-reviewed head `796446b`; prior blockers fixed: project geography is materialized into `scenarioData.project_geography`, and `Process.split_fractions` is canonical in Python and survives parse/model_dump. Remaining blocker is merge conflicts in `belt-ui/src/api/belt.ts` and `belt-ui/src/components/controls/ProcessPanel.tsx`. Tank was delegated to make a resolution commit on top of PR #119 after inspecting main commits since `12741f9`, feature commits, and PR comments/reviews.
  - PR #121 is real Climatiq API execution. Recommended to keep separate from AI gap-fill integration. Climatiq execution should be independently reviewable/mergeable, with a later orchestration PR connecting completed activity data to Climatiq estimates.
- Climatiq status from Kaylee gap analysis: request-shape and response-normalization scaffolding exist, but actual live retrieval needed a client for `/data/v1/estimate`, `CLIMATIQ_API_KEY`, auth/timeout/error handling, orchestration, fixture/live validation, QA CLI, and broader activity-family mappings. Tank was delegated to implement the real Climatiq execution adapter in a fresh PR.
- Martha’s architecture recommendation: real OpenAI SDK invocation should live in `belt-api`, not `src/belt` or UI. Core `src/belt` should keep contracts, schema/payload/parser, and injected-invoke adapter. `belt-api` should own env/config, OpenAI client, `invoke(payload)`, and adapter construction. UI should call API only. Guardrails: `OPENAI_API_KEY` server-only; `openai` dependency in `belt-api`; env-driven model/timeout/retry; deterministic tests with stubbed invoke.
- Debugging/QA: Kaylee was asked for recommendations on codebase additions for debugging the stitched flow: AI gap-fill → process/activity data → emissions derivation → Climatiq execution → UI feedback/audit. Then Kaylee was delegated to implement first QA/debug foundation slice in a fresh PR: IntegrationTraceBundle-style models, redaction/sanitization helpers, correlation/trace ID helpers, JSON serialization/deserialization, deterministic tests/golden fixture, and minimal docs. No raw prompts, full scenario docs, secrets, or full provider payloads by default.
- Persistence: Josh proposed using existing Supabase persistence and storing updated scenario data as `scenario_doc jsonb`. Luther was asked first for an issue-ready architecture recommendation, then delegated to implement a fresh PR using existing `public.scenarios.scenario_doc` jsonb as an authenticated explicit save path. Scope: validate/normalize through Python `Scenario`, ownership checks, no autosave yet, no parallel store, do not persist secrets/raw provider payloads/transient traces, tests/docs.
- Notion monitor convention update from Josh: use the BELT SubAgent Monitor Notion page as canonical monitor. When delegating, update monitor rows, but do NOT add a work-log entry for every delegation. Leave work log for sub-agent responses/completions or non-delegated work done directly by Thoobius.
- OpenAI provider troubleshooting note: if BELT API returns 503 configuring OpenAI gap-fill and the log `Configured AI gap-fill provider` is absent, failure occurs before successful provider construction: likely missing/blank `OPENAI_API_KEY`, invalid timeout/retry env vars, or missing `openai` package. Suggested adding warning log around `OpenAIGapFillProviderConfigurationError` in API dependency. Env var for model is `BELT_AI_GAP_FILL_MODEL`; default/recommended start: `gpt-4o-mini`. Exact model names can be listed with OpenAI `/v1/models` using the deployed API key.
