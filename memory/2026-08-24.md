- 2026-08-24: Added `prep for meeting with Nic` to the Notion `Current Kanban` as `Project = CFB`, `Initiative = Income-Generation`, `Status = queued`, `Priority = 6 - Urgent`, `Due Date = 2026-08-25`, with next step `Draft agenda, goals, and open questions before tomorrow's meeting.`
- 2026-08-24: Added a numeric `Running Time` property to the shared Notion `Movies to Watch Together v4` database and backfilled it from TMDb for all titled films. Verification summary: `71` titled movie rows refreshed, `4` blank placeholder rows skipped, `0` unresolved titles, `0` missing runtimes. Also updated `/home/boovius/.openclaw/workspace/scripts/refresh_movie_streaming.py` so future movie refresh passes keep `Running Time` populated alongside streaming metadata.
- 2026-08-24: Added `prep for meeting with Nic` to the Notion `Current Kanban` as `Project = CFB`, `Initiative = Income-Generation`, `Status = queued`, `Priority = 6 - Urgent`, `Due Date = 2026-08-25`, with next step `Draft agenda, goals, and open questions before tomorrow's meeting.`
- 2026-08-24: Added a numeric `Running Time` property to the shared Notion `Movies to Watch Together v4` database and backfilled it from TMDb for all titled films. Verification summary: `71` titled movie rows refreshed, `4` blank placeholder rows skipped, `0` unresolved titles, `0` missing runtimes. Also updated `/home/boovius/.openclaw/workspace/scripts/refresh_movie_streaming.py` so future movie refresh passes keep `Running Time` populated alongside streaming metadata.
- 2026-08-24: For stakeholder-facing CFB Contribute App UX diagrams, default to a Mermaid flow with a `Contributor Journey` subgraph and a post-submission `Contribution Processing` handoff where `$ - Contribution` becomes the primary entity at the `Terms agreement modal`. Current branch logic to preserve: both signed-in and non-signed-in contributors see `contribution amount and potential match amount`; signed-in users then answer `doing business with the City` and go straight to review/payment, while non-signed-in users go through employment info first, then `doing business with the City`, then personal and billing info before review/payment.
