- BELT emissions progress on 2026-05-30:
  - PR #99 (issue #84 thin Climatiq adapter) merged to main.
  - After merge, issue #84 was closed; issue #83 was also closed administratively as already completed by the contract/readiness seam.
  - PR #76 was closed as superseded by the newer EmissionsActivity-based architecture and PR #99.
  - Kaylee’s recommendation after #99 merged: do not revive/rebase PR #76; only salvage the local emissions QA/debug workflow into a fresh PR.
  - Salvage work opened as issue #107 and PR #108 (`kaylee/issue-pr76-emissions-qa-salvage`) adding `belt emissions-qa ...`, README examples, and request-shape inspection tests on top of the current architecture.
  - Tank opened PR #106 for issue #97 (`tank/issue-97-material-flow-derivation`) implementing a deterministic material-flow derivation seam with single-input/single-output carry-through and split rules plus diagnostics.
  - Luther opened PR #105 for issue #100 (`luther/issue-100-ai-adapter-seam`) implementing a provider-agnostic AI derivation gap-fill seam with bounded structured request/response types and adapter protocol.
  - Luther’s PR #102 for issue #101 was explicitly restacked to only two commits and rebased on main:
    - `f6541c2` Add project geography inheritance seam
    - `c0f6ddc` Validate process geography overrides
- Geography work context:
  - Issue #101 covers project geography inheritance with per-process override.
  - Issue #103 covers geography normalization, provider boundary, and autocomplete UX.
  - Kaylee’s seam concern on PR #102 was override semantics; Tank patched validation so `geography_override=True` cannot be set with missing/null `geography`.
- Multi-agent/ACP context:
  - Josh wants persistent ACP sessions for BELT roles: Tank, Luther, Martha, Kaylee.
  - Attempt to create ACP sessions failed because no configured ACP `agentId` or `acp.defaultAgent` was visible in the environment. This is a configuration/targeting blocker, not a Telegram limitation.
  - BELT repo path confirmed as `/home/boovius/software-development/belt`.
- Notion/movie-list context:
  - Josh used the assistant heavily to update a Notion movie database, including watched status and ratings-related schema changes.
  - There was friction around duplicate-name-field requests and some earlier assistant responses overstated success; be careful to verify actual Notion updates rather than confidently asserting them.
