- Published BELT modeling parent issue #52 in boovius/belt, incorporating Kaylee’s repo-grounded framing, migration guidance, concept note, schema mapping, integration instructions for the current stream, and a Mermaid diagram.
- Added app-structure implications to BELT issue #52: Login, Dashboard, Project starter wizard, Project view, Scenario optimizer, and User profile, aligned with a project-first / scenario-centered direction.
- Martha reviewed #52 and created child issues #53-#58 covering canonical domain roles/invariants, Product vs ProcessOutput, Transfer -> Flow migration, Dependency + derived Stage, app-structure alignment, and docs alignment. Her recommended ordering: #53 first, then #54/#55/#56, then #57, then #58; implementation-critical pair after #53 is #55 and #56.
- Created a sequencing note under the Notion page “Thoobius - BELT” summarizing the recommended order across the project/scenario container line and the newer modeling line.
- Tank implemented BELT issue #49 as PR #59 (branch feat/issue-49-project-container): lightweight Project container above scenarios, project-aware API routes, local project storage, project-first selector flow, and New Project creation while keeping ScenarioData as the canonical modeled payload.
- Kaylee reviewed PR #59 and judged it mergeable with small follow-ups; main caution was that local project naming/persistence is effectively tied to scenario naming and will need cleanup once BELT supports multiple scenarios per project.
- After PR #47 merged, PR #59 needed a real integration pass with main rather than a simple rebase. Main had newer authenticated persisted-scenario work that #59 lacked, including auth-aware scenario/project fetching, POST /scenarios persistence flow, and conflicting UI changes in ScenarioSelector and IntakeWizard.
- Integrated PR #59 onto current main with new commit d81f83e ("Integrate project container with auth-backed scenarios"): preserved main’s auth-backed persisted scenario flow, restored POST /scenarios, added project-aware GET /projects and GET /projects/{project_id}/scenarios, carried project_id through scenario metadata/intake metadata, and merged project-first selector behavior with the signed-in persisted draft flow.
- Validation after the integration pass: belt-ui tests passed, Python compile checks for belt-api files passed, and belt-ui build remained blocked by an existing main-branch @supabase/supabase-js dependency/type issue rather than the project-container integration itself.
