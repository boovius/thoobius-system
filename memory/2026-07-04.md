- Added `Blade Runner` (the 1982 original) to the shared Clara/Josh Notion `Movies to Watch Together v4` database with a full metadata pass: Name Copy, Genre, Year, Country of Origin, Director, Writer, Rotten Tomatoes Score, Metacritic Score, Movie Details URL, Trailer URL, Synopsis, `Who Entered It = Josh`, and `Status = Not watched`.
- Added a Wikimedia poster for `Blade Runner` to the shared Clara/Josh Notion `Movies to Watch Together v4` database, setting both the `Poster` files property and the page cover. After rechecking the whole database, the only remaining rows without posters are three fully blank placeholder entries with no title or metadata.
- Replaced broken Flixster/Rotten Tomatoes poster URLs in the shared Clara/Josh movie database for `Moonstruck`, `Saving Private Ryan`, `The Hidden Fortress`, `The Motorcycle Diaries`, and `Midsommar` with direct Wikimedia image URLs, updating both the `Poster` property and page cover for each.
- Added streaming-availability schema to the shared Clara/Josh movie database: `Streaming On` (multi-select with Netflix, Prime Video, Disney+, Hulu, Apple TV+, Max, Paramount+, Peacock), `Streaming Status` (🟢/🔴/⚪ select), `TMDb ID` (number), and `Last Streaming Refresh` (date).
- Added local refresher script at `/home/boovius/.openclaw/workspace/scripts/refresh_movie_streaming.py`. It reads Notion rows, resolves/stores `TMDb ID`, fetches US flatrate providers from TMDb watch-provider data, writes `Streaming On`, sets `Streaming Status`, and stamps `Last Streaming Refresh`. It currently dry-runs cleanly to a token-missing error because no TMDb credential is configured yet.
- Registered disabled OpenClaw cron job `Refresh movie streaming availability` (`d17a4827-ba19-4d1a-9720-0a22100e6cb2`) for `05:15 America/Los_Angeles` daily. It should remain disabled until a TMDb read token is added at `TMDB_READ_ACCESS_TOKEN` or `~/.config/tmdb/read_access_token`.
- Scheduled a one-shot reminder for Josh for July 5, 2026 at 11:00 AM America/Los_Angeles about the TMDb token setup for the movie streaming refresh. Cron job id: `64ce98df-9a93-4963-8995-719fb2bd4ac8`.
- Josh’s July 4th movie-picking preference: he wants something "American" in theme, not especially kid-forward since there are no kids, and not super violent or horrifying. `A League of Their Own`, `Field of Dreams`, `National Treasure`, and `Apollo 13` were good-fit recommendations in that lane.
