
## Notion MCP setup pause point
- Josh created a Notion integration and stored the Notion key in the Ubuntu environment.
- Josh shared some Notion documents/pages with the integration.
- mcporter is installed and configured locally at `/home/boovius/docs/system/config/mcporter.json` with a `notion` server using `npx -y @notionhq/notion-mcp-server`.
- Initial mcporter config had no `env` block; likely token-passing issue suspected.
- Josh tried both `${NOTION_TOKEN}` interpolation and hardcoding the token directly into mcporter config `env`, but still got `400 Bad Request` errors.
- `mcporter call notion.search --json '{}'` failed with: unknown tool `search` on server `notion`, so `search` is not a valid tool name for this MCP server.
- Next recommended debugging step when resuming: run the Notion MCP server directly outside mcporter with `NOTION_TOKEN` set, inspect raw output, and also run `mcporter list notion` to get the actual tool names.
- Relevant doc created: `/home/boovius/docs/system/notion-mcp-setup-guide.md`.
