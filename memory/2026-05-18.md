- BELT PR #67 received a requested-changes review. A later code-structure pass by Cali decomposed `OpportunityStep.tsx` into smaller pieces: `opportunityConfig.ts`, `OpportunityQuestionCard.tsx`, and `FlowStageToggle.tsx`, simplifying `OpportunityStep.tsx` and reducing repeated markup in `GoalStep.tsx`. Commit: `6ad1d5a`.
- BELT PR #68 was rebuilt cleanly on top of the latest PR #67 tip (`6ad1d5a`). Tank resolved a `SystemMapPreview.tsx` conflict and kept the explicit core/secondary process inputs slice intact. Branch `feat/issue-66-process-inputs-slice` head after rebuild: `586d5a9`.
- User clarified a stronger desired BELT wizard flow direction: (1) core product + one-or-many core processes with at least one required, (2) define inputs/outputs for core-product processes, (3) define waste streams as non-core outputs with multi-select and `to secondary product` inputs, (4) define secondary processes mirroring step 2, then allow looping back to map additional unused outputs or moving onward. This direction should guide the next #68 restructuring pass rather than being treated as mere #67 polish.
- Brine & Ember notes: user wanted the Les Hilliard / Ocean Made meeting captured in Notion. Summary included Ocean Made making kelp-based planter containers using alginate + woody biomass, sourcing kelp waste biomass from Ocean Rainforest, and the relevance to Brine & Ember as an adjacent biomass cascade/value-extraction model. Note created at: `https://www.notion.so/Les-Hilliard-Ocean-Made-meeting-summary-364d409583d181258a6ee400d38d1e9b`.
- On BELT PR #67 review follow-up, one specific inline request was addressed by Tank: `handleDuplicateScenario()` logic was moved out of `belt-ui/src/App.tsx` into `belt-ui/src/hooks/useDuplicateScenario.ts`, with `App.tsx` reduced to using the hook. Commit: `87b324d`. User also emphasized that when handling PR reviews, all comments on the PR should be gathered and sent over, not just one formal review thread comment.
- BELT PR #67 later received a fuller review-resolution pass. Tank addressed remaining targeted comments by extracting more step logic into helpers: `getVisibleOpportunityQuestionIndex(...)`, `getOpportunityQuestionState(...)`, `getOpportunityStepRequiredKeys(...)`, and `canAdvanceOpportunityStep(...)`, and updated `features/intake.ts` so `canAdvanceStep(0, draft)` delegates to helper logic instead of hardcoding the step-0 wizard structure inline. Commit: `4d6aa93` (`Refine intake step helpers`). Targeted tests and build passed.
- User prefers PR review handling to include the complete outstanding comment set, not just the single formal review thread item; this was explicitly corrected during PR #67 handling.
